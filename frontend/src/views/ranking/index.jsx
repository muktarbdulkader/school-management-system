import { Grid, Typography } from '@mui/material';
import React from 'react';

export const Ranking = () => {
  return (
    <Grid>
      <Grid item={12}>
        <Typography variant="subtitle1">Ranking page</Typography>
      </Grid>
    </Grid>
  );
};
